import { StubScreen } from "@/components/ui/stub-screen";

export default function TimelinePage() {
  return (
    <StubScreen
      eyebrow="Timeline"
      title="The record of your adult life"
      ships="M8"
      description="Append-only life events, filterable by type and pillar. Can start in parallel with the daily layer — nothing here depends on scoring or Coach."
    />
  );
}
