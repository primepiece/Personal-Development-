import { StubScreen } from "@/components/ui/stub-screen";

export default function TodayPage() {
  return (
    <StubScreen
      eyebrow="Today"
      title="Daily Prime"
      ships="M2"
      description="Morning: pick up to five actions. Evening: a two-minute freeform review. Both land in M2, once the goal cascade in M1 exists for actions to link up to."
    />
  );
}
