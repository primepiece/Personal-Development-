import { StubScreen } from "@/components/ui/stub-screen";

export default function GoalsPage() {
  return (
    <StubScreen
      eyebrow="Goals"
      title="Vision → 3yr → Annual → Quarterly → Monthly → Weekly"
      ships="M1"
      description="Vision, standards, identity and the full goal cascade — outcomes and recurring behaviours both — are the first real feature, built before any scoring or AI touches the app."
    />
  );
}
