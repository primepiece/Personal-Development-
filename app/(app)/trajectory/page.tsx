import { StubScreen } from "@/components/ui/stub-screen";

export default function TrajectoryPage() {
  return (
    <StubScreen
      eyebrow="Trajectory"
      title="If you keep living exactly like this, where do you end up?"
      ships="M4"
      description="Net Worth, Owned Business Revenue across your ventures, and Half Marathon time — target vs. a computed linear trend from your own logged checkpoints. No projection until there's real data to project from."
    />
  );
}
