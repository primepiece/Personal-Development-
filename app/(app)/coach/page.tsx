import { StubScreen } from "@/components/ui/stub-screen";

export default function CoachPage() {
  return (
    <StubScreen
      eyebrow="Coach"
      title="Evidence, not encouragement"
      ships="M5–M6"
      description="A deterministic signal layer first, then Claude narration grounded in your actual data — every claim expandable into the records behind it. Built last, on top of everything else."
    />
  );
}
